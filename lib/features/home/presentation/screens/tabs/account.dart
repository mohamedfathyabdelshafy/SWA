import 'package:flutter/material.dart';

class AccountTap extends StatelessWidget {
  const AccountTap({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
